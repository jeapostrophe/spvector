#lang setup/infotab
(define name "Semi-persistent Vectors")
(define release-notes
  (list))
(define repositories
  (list "4.x"))
(define blurb
  (list "'Functional' vectors that allow persistent reads but not persistent writes"))
(define scribblings '(("spvector.scrbl" ())))
(define primary-file "main.ss")
(define categories '(datastructures))